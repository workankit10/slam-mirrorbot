name: Manually Deploy to Heroku

on: workflow_dispatch

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: akhileshns/heroku-deploy@v3.12.12
        with:
          heroku_api_key: 29242e2e-e772-4daa-ab84-87b4c387e65c
          heroku_app_name: muthubhai
          heroku_email: muthubhai123@mail.com
          usedocker: true
          docker_heroku_process_type: web
          stack: "container"
          region: "eu"
        env:
          HD_CONFIG_FILE_URL: https://gist.githubusercontent.com/workankit10/4a566f571ecd3712949ad37b240befe1/raw/config.env
